# to_do/ — Task Specs

- **Naming**: `P{0-3}_{PRIORITY}_{name}.md` — priority prefix determines order
- **Lifecycle**: Move completed files to `recycle_bin/`
- **Context**: Each task must be self-contained — write for a session with no prior memory
