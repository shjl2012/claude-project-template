# scripts/ — Standalone Scripts

- **Imports**: Scripts import from `src/`. Do not duplicate logic here.
- **Execution**: Run from project root with `python scripts/[name].py`.
- **Side effects**: Scripts that modify data or external systems should log their actions.
