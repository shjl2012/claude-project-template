# tests/ — Test Suite

- **Mirrors `src/`**: Test files should correspond to source modules
- **Fixtures**: Shared fixtures go in `conftest.py`, not duplicated across test files
- **Run**: `pytest tests/` from project root
