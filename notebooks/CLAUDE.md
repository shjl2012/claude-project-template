# notebooks/ — Jupyter Notebooks

- **Exploration only**: Notebooks are for prototyping. Extract proven logic into `src/`.
- **Data paths**: Use relative paths from the project root (e.g., `../data/raw/`).
- **Outputs**: Clear cell outputs before committing to keep diffs readable.
