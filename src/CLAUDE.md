# src/ — Source Code

Changes here may affect `tests/` and `scripts/`. When modifying public interfaces (function signatures, class APIs), check downstream consumers.

- **Imports**: Use absolute imports from the package root
- **Dependencies**: All third-party dependencies must be declared in `pyproject.toml`
