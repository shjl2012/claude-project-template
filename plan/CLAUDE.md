# plan/ — Code Planning

- **Workflow**: Plan here first, then implement in `src/` or `scripts/`
- **Cleanup**: Move completed/abandoned plans to `recycle_bin/`
- **No deletion**: Files are moved, not deleted
